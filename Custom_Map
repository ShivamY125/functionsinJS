function main() {
    function customMap(array, callbackfn) {

         const output = [];
        for(let i=0;i<array.length;i++){
            output.push(callbackfn(array[i]));
        }
       return output;
    }
    return customMap;
  }
  
